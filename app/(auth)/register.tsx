/**
 * Register screen — US-AUTH-02. Skeleton; full UI to be polished alongside
 * prototype integration.
 */
import { View } from 'react-native';

import { Heading, Screen } from '@/shared/ui';

export default function RegisterScreen() {
  return (
    <Screen contentClassName="px-6 pt-12">
      <Heading level="h1">Inscription</Heading>
      <View className="mt-4" />
    </Screen>
  );
}
